/***************************************************************************************************************************************************************
 *
 * image min
 *
 * Minify images
 *
 **************************************************************************************************************************************************************/

module.exports = function imagemin(grunt) {
  var imagemin = {
    build: {
      files: [{
        expand: false,
        cwd: '.',
        src: ['dist/**/*.{png,jpg,gif}'],
        dest: 'dist/src/img/build/'
      }]
    }
  };

  grunt.config.set('imagemin', imagemin);

};
