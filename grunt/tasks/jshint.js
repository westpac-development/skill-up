/***************************************************************************************************************************************************************
 *
 * js linting and report
 *
 *
 **************************************************************************************************************************************************************/

module.exports = function jshint(grunt) {
	var jshint = {
		all: ['Gruntfile.js', './dist/*.js']
	};
	grunt.config.set('jshint', jshint);
};
