/***************************************************************************************************************************************************************
 *
 * W3C Validation for the html files.
 *
 **************************************************************************************************************************************************************/

module.exports = function validate(grunt) {
 var validate = {
  htmllint: {
   all: ["./**/*.html"]
  }
 };

 grunt.config.set('grunt-html', validate);
};
