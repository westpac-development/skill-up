/***************************************************************************************************************************************************************
*
* Html validation
*
* Validate all html files
*
**************************************************************************************************************************************************************/

module.exports = function html(grunt) {
	var htmllint = {
	    all: ["./**/*.html"]
	};

	grunt.config.set('htmllint', htmllint);
};
